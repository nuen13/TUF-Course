#include<bits/stdc++.h>
using namespace std;


void unorderedSet(){

    /*
        Unordered_set 

        + Unique
        + NOT Sorted

        + All operation is the same as others and is O(1)
    
    */


    unordered_set<int> uset;

    // lower_bound and upper_bound function -> does not work 
    // rest all functions are the same 
    // does not stores in any particular order as it has a
    // better complexity than set in most cases, except some 
    // when collision happens

}

